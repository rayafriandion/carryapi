# carryAPI 使用手册

> carryAPI 是一个自托管的 API 聚合路由服务：把多个上游模型供应商（OpenAI 兼容、Anthropic 等）聚合，对外统一暴露自定义模型名，提供可视化配置、用量/费用/成功率统计、多用户认证。
>
> 本文档是**完整操作手册**，覆盖安装部署、全部可配置项、管理后台每个页面的操作、对外 API、认证与运维。**以后每新增或减少一个功能，必须同步更新本文档**（见「维护约定」）。

---

## 目录

- [1. 概述](#1-概述)
- [2. 安装与构建](#2-安装与构建)
- [3. 启动方式](#3-启动方式)
- [4. 首次启动与管理员](#4-首次启动与管理员)
- [5. 配置](#5-配置)
- [6. 管理后台总览](#6-管理后台总览)
- [7. 前端操作指南（逐页说明）](#7-前端操作指南逐页说明)
- [8. 模型 / 供应商 / 定价 / 路由配置](#8-模型--供应商--定价--路由配置)
- [9. 代理端点（对外 API）](#9-代理端点对外-api)
- [10. 统计与日志](#10-统计与日志)
- [11. 认证体系](#11-认证体系)
- [12. 运维](#12-运维)
- [13. 常见问题](#13-常见问题)
- [14. 维护约定](#14-维护约定)

---

## 1. 概述

### 核心能力

- **多协议聚合代理**：客户端可用 OpenAI Chat / Responses / Anthropic 三种协议之一调用，代理自动转换为上游支持的协议。上游同样支持这三种协议任意组合。
- **多供应商路由**：一个对外模型名可绑定多个供应商 + 上游模型，按优先级、权重、故障转移、健康感知等策略选路。
- **多用户认证**：邮箱+密码、TOTP 2FA、Passkey（WebAuthn）、OAuth（Discord / X）。
- **API Key**：每个用户可创建多个带标签的 Key，用于调用代理端点。
- **配额管理**：按用户或 Key 设置 token / 费用上限（按日 / 月 / 总计周期）。
- **用量统计**：输入/输出/缓存 token、费用、成功率，按模型/上游/Key 维度 + 时间趋势。
- **请求日志**：每次请求明细，支持分页与多条件筛选。
- **模型目录**：所有登录用户可只读浏览启用模型、价格、绑定、健康状态与性能指标。
- **单二进制部署**：前端已内嵌，无需单独部署前端。

### 技术栈

| 层 | 技术 |
|----|------|
| 后端 | Go（单二进制，无 CGO） |
| 前端 | Vue 3 + Naive UI + Vite + Pinia + ECharts |
| 数据库 | SQLite（单文件，纯 Go 驱动 modernc.org/sqlite） |
| 认证 | bcrypt、TOTP（RFC 6238）、WebAuthn、OAuth 2.0 |

### 跨平台

支持 **Windows（x64）** 与 **Linux（x64）**，无 CGO，交叉编译即可。启动脚本提供 `.bat`（Windows）与 `.sh`（Linux / macOS / git-bash）两套。

---

## 2. 安装与构建

### 前置要求

- **Go 1.22+**
- **Node.js**（仅构建前端时需要；运行单二进制不需要）

### 构建

```bash
# 1. 构建前端（产物 web/dist）
cd web && npm install && npm run build && cd ..

# 2. 构建后端单二进制（内嵌前端）
go build -o carryapi ./cmd/carryapi

# Windows 交叉编译：
# GOOS=windows GOARCH=amd64 go build -o carryapi.exe ./cmd/carryapi
# Linux 交叉编译：
# GOOS=linux   GOARCH=amd64 go build -o carryapi-linux-amd64 ./cmd/carryapi
```

> 前端只影响 `web/dist/`，后端构建时通过 `embed` 自动嵌入。

### 测试

```bash
# Go 全量测试
go test ./...

# 前端测试
cd web && npm run test

# 或一次跑全部
make test
```

---

## 3. 启动方式

> **统一启动**：提供了跨平台脚本，Windows 与 Linux 通用，不需要手动点击 exe 文件。

### 生产模式（推荐）

一条命令构建并启动（默认端口 **8067**）：

```bash
# Linux / macOS / git-bash
bash scripts/run.sh

# Windows（cmd 或双击）
scripts\run.bat

# 指定端口
bash scripts/run.sh 9000
scripts\run.bat 9000
```

等价于手动执行：

```bash
go build -o carryapi ./cmd/carryapi
./carryapi
```

生产模式是**单进程**：前端已内嵌在后端二进制里，启动后访问 `http://localhost:8067/` 即是完整管理后台。

### 开发模式（前后端同时热更新）

```bash
# Linux / macOS / git-bash：同时启动前端 Vite + 后端
bash scripts/dev.sh

# Windows
scripts\dev.bat
```

- 前端开发服务器：`http://localhost:5173`（热更新，`/api` `/v1` 自动代理到后端 8067）
- 后端：`http://localhost:8067`
- 停止：按 `Ctrl+C`（`dev.bat` 需关闭前端窗口）。

### Makefile（可选）

有 `make` 的环境可用统一入口：

```bash
make run       # 构建并运行（端口可用 make run PORT=9000 覆盖）
make dev       # 开发模式
make test      # 全部测试
make build     # 仅构建
make clean     # 清理构建产物
```

---

## 4. 首次启动与管理员

首次启动（无 `carryapi.db` 时）自动：

1. 创建数据库与全部表（含版本化迁移）。
2. 生成主密钥文件 `carryapi.key`（如未设置 `CARRYAPI_MASTER_KEY` / `CARRYAPI_KEY_FILE`）。

**首次启动不会自动创建管理员或打印随机密码。** 访问 `http://localhost:8067/` 时，由于库中尚无管理员，页面会自动进入**「首次设置」向导**，由你填写管理员邮箱与密码来创建首个 admin 账户。创建成功后用该账号登录管理后台。**首次登录后建议立即开启 TOTP 两步验证**（账号设置页）。

### 脚本化部署

若希望在无人值守环境自动创建管理员（不经过向导），需**同时**设置以下两个环境变量：

- `CARRYAPI_ADMIN_EMAIL`
- `CARRYAPI_ADMIN_PASSWORD`

当且仅当两者都设置且库中无 admin 时，启动时自动创建管理员；否则一律交由网页向导处理。仅设置其一无效。

### 管理员身份保护

- 首个 admin（向导创建的初始管理员）账户不可被**降级**、**禁用**或**删除**。
- 任何管理员都不可降级/禁用/删除**自己**。
- 其他管理员之间可以互相调整角色（但不能动自己）。
- 管理员可把其他用户设为管理员（用户管理页「角色」改为「管理员」）。

---

## 5. 配置

### 环境变量

所有环境变量均为可选，有内置默认值：

| 变量 | 默认 | 说明 |
|------|------|------|
| `CARRYAPI_HOST` | `all` | 监听地址：`all`=双栈监听 `[::]`（通常含 IPv4），`0.0.0.0`=仅 IPv4，`::`=IPv6 双栈，`127.0.0.1`/`::1`=仅本机 |
| `CARRYAPI_PORT` | `8067` | 监听端口 |
| `CARRYAPI_DB_PATH` | `./carryapi.db` | SQLite 数据库文件路径 |
| `CARRYAPI_MASTER_KEY` | 自动生成 | 敏感字段（上游 API Key、TOTP、Passkey、OAuth 令牌）加密主密钥，**必须恰好 32 字节** |
| `CARRYAPI_KEY_FILE` | `carryapi.key` | 主密钥文件路径。未设置 `CARRYAPI_MASTER_KEY` 时读取该文件；文件不存在则自动生成随机 32 字节密钥并写入（权限 0600） |
| `CARRYAPI_ADMIN_EMAIL` | — | 脚本化部署：自动创建管理员邮箱（需与密码同设） |
| `CARRYAPI_ADMIN_PASSWORD` | — | 脚本化部署：自动创建管理员密码（需与邮箱同设） |
| `CARRYAPI_RP_ID` | `localhost` | WebAuthn Passkey 的 Relying Party ID（公网部署改为你的域名） |
| `CARRYAPI_RP_ORIGIN` | `http://localhost:<port>` | WebAuthn origin（公网部署必须改为你的 HTTPS 地址，如 `https://api.example.com`） |

> **主密钥务必妥善备份**：丢失后数据库中加密的上游 API Key 等将无法解密。`CARRYAPI_MASTER_KEY` 与 `CARRYAPI_KEY_FILE` 二选一，同时设置时优先使用 `CARRYAPI_MASTER_KEY`。

### 数据库 settings 表（后台可改的配置）

部分配置存在 SQLite 的 `settings` 键值表中，通过管理后台「系统设置」页修改：

| 键 | 含义 | 默认（未设置时） | 是否实际生效 |
|----|------|------------------|--------------|
| `listen_host` | 监听模式：`all`=双栈广播开，`0.0.0.0`=仅IPv4，`::`=IPv6双栈，`127.0.0.1`/`::1`=仅本机 | `all` | ✅ 启动时读取 |
| `registration_open` | 是否允许公开自助注册 | 未设置时按开放处理 | ✅ 注册/OAuth 回调时校验 |
| `force_2fa` | 是否强制所有账户开启 TOTP | — | ⚠️ **当前版本仅保存与展示，未在登录流程中强制执行** |
| `log_retention_days` | 请求日志保留天数 | — | ⚠️ **当前版本仅保存与展示，暂无自动清理任务** |

> `force_2fa` 与 `log_retention_days` 两个开关在界面可设置、会持久化，但对应逻辑尚未在后端实现，**不产生实际效果**，属于预留项。请勿依赖其行为。

### 广播开关（监听地址）

管理后台「系统设置」可切换广播开关并保存到 `settings.listen_host`，**重启 carryAPI 后生效**。支持的值：

| 值 | 含义 |
|----|------|
| `all` | 默认。监听 `[::]`，通常同时支持 IPv6 与 IPv4-mapped 访问 |
| `0.0.0.0` | 仅监听全部 IPv4 接口 |
| `::` | 监听 `[::]`，通常同时支持 IPv6 与 IPv4-mapped 访问 |
| `127.0.0.1` | 仅本机 IPv4 |
| `::1` | 仅本机 IPv6 |

也可以通过启动参数或环境变量指定监听地址，优先级高于后台设置：

```bash
./carryapi --host all --port 8067
./carryapi --host 127.0.0.1
CARRYAPI_HOST=:: CARRYAPI_PORT=8067 ./carryapi
carryapi.exe --host all --port 8067
```

当使用 `--host` 或 `CARRYAPI_HOST` 时，后台广播开关会显示为锁定状态，并提示配置来源。IPv6 公网访问时 URL 必须写成 `http://[你的IPv6]:8067/`；还需确认地址是公网 GUA 地址（通常是 `2` 开头，不是 `fe80:`/`fd` 开头），并放行 Windows 防火墙、路由器/光猫的 IPv6 入站防火墙。该值还会影响仪表盘展示的 API base_url 与启动横幅。

### OAuth 配置

Discord / X 登录需要三组配置：`oauth_<provider>_client_id`、`oauth_<provider>_client_secret`、`oauth_<provider>_redirect_url`（`<provider>` 为 `discord` 或 `x`）。当且仅当三者都存在时，对应登录按钮才启用。

> **当前版本后台界面没有配置 OAuth 密钥的表单**，这些键只能直接写入数据库 `settings` 表，修改后需重启进程生效。公网部署还需把回调地址注册到对应平台：
> - Discord：`https://<你的域名>/api/auth/oauth/callback/discord`
> - X：`https://<你的域名>/api/auth/oauth/callback/x`

---

## 6. 管理后台总览

访问 `http://<host>:8067/`。左侧菜单按角色显示：

| 页面 | 路径 | 功能简述 | 权限 |
|------|------|----------|------|
| 首次设置向导 | `/setup` | 创建首个管理员 | 公开（无管理员时） |
| 登录 / 注册 | `/login` | 密码、TOTP、Passkey、Discord/X OAuth、自助注册 | 公开 |
| 仪表盘 | `/` | 今日核心指标、趋势图、最近日志、API base_url | 登录 |
| 模型列表 | `/models-catalog` | 只读浏览启用模型、价格、绑定、健康/性能 | 登录 |
| 模型详情 | `/models-catalog/:id` | 单模型概览、30 天统计、绑定健康时间轴 | 登录 |
| 统计分析 | `/stats` | 汇总、趋势、费用、成功率（可下钻、导出 CSV） | 登录 |
| 请求日志 | `/logs` | 分页 + 多条件筛选 | 登录 |
| API Key | `/keys` | 创建（明文仅显示一次）、编辑、禁用、删除 | 登录 |
| 账号设置 | `/account` | 基本信息、TOTP 开关 | 登录 |
| 模型管理 | `/models` | 供应商增删改查/连通性测试、模型增删改/批量导入 | admin |
| 新建 / 编辑模型 | `/models/new`、`/models/:id/edit` | 模型信息、绑定、定价、路由策略 | admin |
| 路由配置 | `/routing` | 按模型管理绑定、策略、查看健康时间轴与 24h 指标 | admin |
| 配额管理 | `/quotas` | 设置 token / 费用上限 | admin |
| 用户管理 | `/users` | 创建、改角色、禁用、删除用户 | admin |
| 系统设置 | `/settings` | 广播开关、监听模式、开放注册、强制 2FA、日志保留天数 | admin |

> **权限说明**：普通用户登录后只看到前 8 项；admin 额外看到「管理员设置」分组（模型管理、路由配置、配额管理、用户管理）和「系统设置」。数据权限上，普通用户只能看到**自己**的统计、日志、Key、配额；admin 可看全部数据。

---

## 7. 前端操作指南（逐页说明）

### 7.1 首次设置向导（`/setup`）

仅当数据库中没有任何管理员时，访问任意页面会自动跳到这里。

- 表单字段：**邮箱**、**密码**（至少 8 位）、**确认密码**（需与密码一致）。
- 点击「创建管理员」提交，成功后跳到登录页。
- 该接口在已有管理员后自动关闭（返回 403）。

### 7.2 登录 / 注册（`/login`）

页面顶部有「登录」「注册」两个标签。

**登录标签：**
- 输入邮箱、密码点击「登录」。
- 若账号开启了 TOTP，密码框会切换为「验证码」输入框，输入 6 位动态码（或一次性备份码）后点「验证」。
- **Passkey 登录**：在「Passkey 邮箱」框输入邮箱，点「使用 Passkey 登录」，浏览器会弹出指纹/安全密钥验证（需浏览器支持 WebAuthn）。
- **OAuth 登录**：点 Discord 或 X 按钮，浏览器跳转到对应平台授权，授权后回调并自动登录。
- 连续 5 次 TOTP 验证失败会锁定该账号 15 分钟；登录/注册/2FA 接口有基于 IP 的限流（每分钟 10 次）。

**注册标签：**
- 输入邮箱、密码（至少 8 位）点「创建账户」。仅当管理员开启「开放注册」时可用，否则提示注册已关闭。
- 注册成功后自动切回登录标签。

### 7.3 仪表盘（`/`）

- **API Base URL 卡片**（仅 admin 可见）：显示网关对外的 OpenAI 兼容端点地址，形如 `http://<host>:8067/v1`，点「复制」一键复制。客户端直接用它作为 base URL。
- **6 个统计卡片**：总请求数、成功数、失败数、Token 输入/输出、费用、平均耗时（ms）。
- **最近请求趋势图**：按天的请求数 / 成功数折线图。
- **最近日志表**：最近 10 条请求（时间、RequestID、模型、状态码、错误类型、耗时），点击左侧「日志」菜单看完整列表。

### 7.4 模型列表（`/models-catalog`）

所有登录用户可见的**只读**模型目录，展示已启用模型。

- 右上角可切换**卡片视图 / 列表视图**。
- 卡片/每行显示：模型名、币种标签（￥/$）、成功率标签（≥95% 绿、≥80% 橙、>0% 红）、上游绑定数量与名称、输入/输出/缓存读取价格（每百万 token）、平均延迟、近 30 天请求数与成功数。
- 点击任意卡片或行进入**模型详情**页。
- 绑定标签灰色表示该绑定已启用，橙色表示已禁用。

### 7.5 模型详情（`/models-catalog/:id`）

- 顶部「← 返回」回到列表；模型名旁显示启用/禁用标签；**admin 可见「编辑模型」**按钮，进入编辑页。
- **概览**：名称、币种、路由策略（随机使用 / 自动路由-优先级/故障转移/健康感知）、各项价格、创建时间、绑定数量。
- **近 30 天统计**：总请求、成功数、成功率、平均延迟。
- **上游绑定表**：每个绑定显示供应商、协议、上游模型名、优先级、权重、24h 成功率、24h 延迟、24h 请求数，以及**6 格健康时间轴**（每格 4 小时、覆盖最近 24 小时）。鼠标悬停每格显示该时段状态。

### 7.6 统计分析（`/stats`）

四个标签页：

1. **汇总**：三张表，分别按模型、上游 Provider、API Key 列出请求数、输入/输出 Token、费用。
2. **趋势**：按天/按小时切换；双轴图（柱=请求数，线=成功数、成功率%）。
3. **费用**：按模型分组的请求数与费用表，右上角「导出 CSV」下载 `费用_<日期>.csv`。
4. **成功率**：按模型列出总数、成功、失败、成功率、平均耗时。「失败」列是可点击按钮（为 0 时禁用），点开**失败类型明细**弹窗，按错误类型聚合数量。右上角「导出 CSV」下载 `成功率_<日期>.csv`。

> 时间范围默认为最近 30 天。普通用户只看到自己的数据。

### 7.7 请求日志（`/logs`）

- **筛选栏**（6 个可清空输入框，回车即查询）：模型、错误类型、RequestID、状态码、开始时间、结束时间（RFC3339 格式）。
- 「查询」按钮按条件加载第 1 页；「重置」清空所有条件并重新加载。
- 表格列：时间、RequestID、用户、模型、上游模型、协议入/出、输入/输出 Token、费用、耗时、状态码、错误类型。
- 底部分页，每页 20 条，显示总条数。

### 7.8 API Key 管理（`/keys`）

管理当前用户自己的 Key。

- 点「新建 API Key」，在弹窗填**标签**（必填）后创建。
- 创建成功后弹出**一次性明文**窗口，完整 Key 仅在此显示一次。请立即点「复制」保存，点「我已保存」后无法再查看明文。
- 每行可「编辑」（改标签、切换启用/禁用状态）或「删除」（删除前有确认弹窗）。
- 表格显示 Key 前缀、标签、状态、创建时间、最后使用时间。

### 7.9 账号设置（`/account`）

- **基本信息**：用户 ID、邮箱、角色、状态、已绑定的认证方式。
- **两步验证（TOTP）**：
  - 未开启时点「开启 TOTP」，弹窗显示 **otpauth URL**、**手动密钥**和若干**一次性备份码**。用 Authenticator App 扫码或手动录入；点「复制」把 URL、密钥、备份码整体复制保存。备份码每个只能用一次。
  - 已开启时需输入当前密码才能「关闭 TOTP」。关闭 TOTP 会吊销该用户所有会话（需重新登录）。
- 本页不提供修改邮箱或密码的功能。

### 7.10 模型管理（`/models`）【admin】

两个标签页。

**供应商标签：**
- 「新建供应商」弹窗字段：**名称**、**Base URL**（如 `https://api.openai.com/v1`）、**API Key**（密码框）、**协议**（OpenAI Chat / OpenAI Responses / Anthropic）。
- 每行有「测试」按钮：调用供应商上游检查连通性，显示「可用 <延迟>ms」（绿）或「不可用: <原因>」（红）。
- 「编辑」可改名称、Base URL、协议、状态（启用/禁用）；API Key 框留空表示不修改。
- 「删除」有确认弹窗。删除供应商前需先解除其下模型绑定。
- API Key 经 AES-GCM 加密存储，任何接口都不返回明文。

**模型标签：**
- 「新建模型」跳转到模型编辑页；「从供应商导入」打开批量导入弹窗。
- 导入弹窗：先选供应商，系统拉取该供应商的上游模型列表；已存在的模型标「已存在」且禁用勾选；勾选要导入的模型后点「导入 (N)」。导入的模型以**禁用草稿**创建，之后需配置价格、绑定并启用才能调用。
- 模型列表每行有名称、**启用开关**（可直接切换）、编辑、删除（确认弹窗）。

### 7.11 新建 / 编辑模型（`/models/new`、`/models/:id/edit`）【admin】

表单分三块：

1. **基本信息**
   - **名称**：对外暴露的模型名（客户端 `model` 字段填这个），必填。
   - **启用**：开关，默认开启。
   - **路由策略**：`自动路由 auto`（默认）或 `随机使用 random`。
   - **自动模式**（仅策略为自动时显示）：`优先级 priority`（默认）、`故障转移 failover`、`健康感知 health`。
2. **上游绑定**
   - 点「添加绑定」新增一行。每行字段：**供应商**（可搜索下拉）、**上游模型**（发给上游的真实模型名）、**优先级**（数字，越小越优先，默认 100）、**权重**（数字，同优先级内加权随机，默认 1）、**启用**开关。
   - 至少保留一条绑定（最后一条不可删除）。
   - 路由时实际发给上游的 `model` 字段会替换为绑定里的上游模型名。
3. **定价**（按每百万 token 计）
   - **币种**：美元 USD / 人民币 CNY（默认 USD），必填。
   - **输入价格**、**输出价格**：必填，非负。
   - **缓存读价格**、**缓存写价格**：可选，留空不计。
   - 保存时前端会校验名称、币种、输入/输出价格及每个绑定的合法性。

> 编辑保存时会分别更新模型基本信息/价格/路由，并对绑定逐条增删改。调价会新增一条带生效时间的价格历史行，旧账单按当时价格不变。

### 7.12 路由配置（`/routing`）【admin】

以模型为主视图管理绑定与健康状态。

- 顶部「刷新」按钮；提示「6 格 = 4 小时/格」。
- 每个模型可展开，展开后显示其绑定表：供应商、上游模型、优先级、权重、**24h 时间轴**（6 格彩色：绿≥95%、橙80–95%、红<80%、灰=无数据）、平均延迟、**启用开关**（可直接切换）、操作。
- 行操作：
  - **添加绑定**：弹窗字段同模型编辑页的绑定（供应商、上游模型、优先级、权重、启用；此页权重默认 100）。
  - **路由策略**：弹窗修改该模型的路由策略与自动模式。
  - 每个绑定的「详情」：打开 **24h 指标详情**弹窗，显示平均延迟、平均 TTFT（首字延迟）、每小时吞吐量、24h 请求数、成功率。
  - 「编辑」「删除」（删除有确认；最后一条绑定不可删）。

> 健康状态由后台每 1 分钟从真实请求日志聚合一次，因此页面数据最多有 1 分钟延迟，路由器读缓存不直接查库。

### 7.13 配额管理（`/quotas`）【admin】

- 表格列出配额：范围、范围 ID、周期（day / month / total）、Token 上限、费用上限、已用 Token、已用费用。
- 点「编辑」打开弹窗：**Token 上限**与**费用上限**为数字输入框，**留空表示不限制**。范围与周期不可在此更改。
- ⚠️ **当前限制**：后端 `GET /api/quotas` 只返回**当前登录用户自己**的配额，因此本页实际编辑的是当前 admin 自身的配额，暂不能给其他用户或指定 Key 设置配额。页面顶部有相应提示。

### 7.14 用户管理（`/users`）【admin】

- 「新建用户」弹窗：邮箱、密码（至少 8 位）、角色（用户 user / 管理员 admin，默认用户）。
- 每行可「编辑」（邮箱只读，可改角色和状态：正常/禁用）或「删除」（确认弹窗）。
- 禁用用户会立即吊销其所有会话。
- 自己这一行显示「当前用户」标记，不可删除自己；首个管理员不可被降级/禁用/删除。

### 7.15 系统设置（`/settings`）【admin】

- **广播开关**卡片：可切换“允许其他设备访问”。默认监听模式为 IPv4 + IPv6 全部接口；保存后需重启 carryAPI 生效。若服务使用 `--host` 或 `CARRYAPI_HOST` 启动，开关会锁定并显示配置来源。
- **一般设置**卡片：
  - **开放注册**（`registration_open`）：是否允许新用户自助注册。✅ 实时生效。
  - **强制两步验证**（`force_2fa`）：⚠️ 当前仅保存，未强制执行。
  - **日志保留天数**（`log_retention_days`，默认 30）：⚠️ 当前仅保存，暂无自动清理。
- 点「保存设置」提交。开关值在传输时为字符串 `"true"`/`"false"`。

---

## 8. 模型 / 供应商 / 定价 / 路由配置

要让代理端点可用，需按以下顺序配置（均需 admin）：

1. **添加上游供应商**（模型管理 → 供应商）：填名称、base_url、API Key、协议。用「测试」按钮验证连通性。
2. **添加自定义模型**（模型管理 → 新建模型，或从供应商批量导入）：定义对外模型名，至少添加一条上游绑定。
3. **配置定价**（模型编辑页 → 定价）：设置币种与输入/输出/缓存价格。
4. **设置路由策略并启用模型**。

### 路由策略详解

每个模型有 `routing_strategy`（`auto` / `random`）与 `auto_mode`（`priority` / `failover` / `health`）：

- **自动路由 + 优先级**（默认）：取优先级数值最小的一组绑定，组内按 `weight` 加权随机。
- **自动路由 + 故障转移**：按优先级从低到高逐个尝试，遇到网络错误或上游返回 **429 / 5xx** 时自动切换到下一个绑定。
- **自动路由 + 健康感知**：优先选择缓存健康状态非"异常"的绑定；若全部异常则回退到全部绑定，再按优先级/权重选择。
- **随机**：忽略优先级，在所有启用绑定间按 `weight` 加权随机。

候选绑定会过滤：必须启用、其供应商必须为 active；健康模式下排除不健康绑定。每条绑定的 `priority` 越小越优先，`weight` 用于同优先级加权随机。

### 健康与性能指标

- **6 格时间轴**覆盖最近 24 小时，每格 4 小时，按该时段成功率着色：🟢 ≥95%、🟡 80–95%、🔴 <80%、⚪ 无流量。
- **客户端主动断开**（HTTP 200 但流中途断开，`error_type=client_disconnect`）既不算成功也不算失败。
- 绑定详情展示 24h **平均响应时延**、**平均 TTFT**（首字延迟）、**每小时吞吐量**、**成功率**，仅作运维参考，不影响时间轴颜色。
- 健康数据每 1 分钟预计算一次。

---

## 9. 代理端点（对外 API）

客户端调用代理端点，需携带 API Key，支持两种鉴权头：

- `Authorization: Bearer <key>`（OpenAI 风格）
- `x-api-key: <key>`（Anthropic 风格）

| 端点 | 方法 | 下游协议 | 说明 |
|------|------|----------|------|
| `/v1/chat/completions` | POST | OpenAI Chat | 标准 Chat 补全 |
| `/v1/completions` | POST | OpenAI Chat（旧别名） | 同上，走 Chat 管线 |
| `/v1/responses` | POST | OpenAI Responses | Responses API |
| `/v1/messages` | POST | Anthropic Messages | Anthropic 格式 |
| `/v1/models` | GET | — | 列出启用的自定义模型（OpenAI `{"object":"list"}` 格式，`owned_by:"carryapi"`） |

**响应格式由请求路径决定**（Chat 请求返 Chat 格式，Responses 返 Responses 格式，Anthropic 返 Anthropic 格式），与上游实际协议无关——协议在内部通过统一中间表示（IR）互相转换。错误也按客户端协议格式返回。

### 调用示例

```bash
curl http://localhost:8067/v1/chat/completions \
  -H "Authorization: Bearer carry-xxxx..." \
  -H "Content-Type: application/json" \
  -d '{"model":"my-gpt4","messages":[{"role":"user","content":"hi"}]}'
```

> `model` 字段填**自定义模型名**（如 `my-gpt4`），代理会映射到上游真实模型并转换协议；实际发给上游的模型名由绑定决定。

列出模型：

```bash
curl -H "Authorization: Bearer carry-xxxx..." http://localhost:8067/v1/models
```

### API base_url

仪表盘（admin）展示网关自身的 OpenAI 兼容端点 `http://<host>:<port>/v1`，可一键复制，客户端直接用它作为 base URL。广播开启时按请求 Host 生成，IPv6 Host 会自动使用 `[IPv6]:端口` 格式；仅本机模式为 `127.0.0.1:<port>`。

### 流式（SSE）与错误行为

- 请求带 `"stream": true` 时，响应为 `text/event-stream`，逐事件转发并实时 flush；服务端不设写超时，长连接不会被中断。
- 上游 HTTP 状态码**原样透传**给客户端（429→429、401→401、5xx→5xx），保留重试语义；错误类型映射为 `rate_limit` / `authentication` / `permission` / `not_found` / `invalid_request`。
- 故障转移仅在 `auto + failover` 策略下、遇到网络错误或 429/5xx 时触发。
- 每次请求都会获得 `X-Request-Id` 响应头并写入 `request_logs`（鉴权失败也会记录，此时用户/Key 可能为空）。

### 配额超限

调用时同时检查**用户**与 **API Key** 两个范围的配额（token 和/或费用上限）。任一超限返回 `429 quota_exceeded`。没有配额记录视为不限制。

---

## 10. 统计与日志

管理后台「统计分析」「请求日志」页提供可视化查询，底层是以下只读 API（需登录 session，GET 无需 CSRF）：

| 端点 | 说明 |
|------|------|
| `GET /api/stats/summary` | 汇总：总请求/成功/失败/token/费用/平均耗时，按模型/上游/Key 分列 |
| `GET /api/stats/trend` | 时间趋势（按天/小时） |
| `GET /api/stats/cost` | 费用核算，按模型/Key/上游分组 |
| `GET /api/stats/success-rate` | 成功率（2xx 且无 error_type 才算成功）与平均耗时 |
| `GET /api/logs` | 请求日志分页 + 筛选 |

公共参数：

- `start` / `end`：时间范围，RFC3339；缺省最近 30 天（UTC）。
- `granularity`：`day`（默认）或 `hour`（trend）。
- `group`：`model`（默认）/`key`/`provider`（cost、success-rate）。
- 日志：`page`（默认 1）、`page_size`（默认 50，上限 200）、`model`、`status`、`error_type`、`request_id`。
- admin 可传 `user_id` 过滤指定用户；普通用户被强制限定为自己的数据。

此外，模型目录与健康页使用：

| 端点 | 说明 |
|------|------|
| `GET /api/catalog` | 列出启用模型，含价格、绑定、30 天全局成功率/延迟（不暴露密钥） |
| `GET /api/catalog/{id}` | 单模型详情，含每绑定 24h 指标 |
| `GET /api/routing/status` | 每个启用模型的全部绑定 + 6 格时间轴/24h 延迟（admin） |
| `GET /api/routing/bindings/{id}/metrics` | 单绑定 24h 详细性能指标（admin） |
| `GET /api/gateway/info` | 返回网关 base URL（admin） |

---

## 11. 认证体系

### 登录方式

一个账号可绑定多种登录方式（账号设置页管理）：

| 方式 | 说明 |
|------|------|
| 密码 | bcrypt 存储 |
| TOTP 2FA | RFC 6238，Authenticator App；提供一次性备份码；连续 5 次失败锁定 15 分钟 |
| Passkey | WebAuthn，指纹/安全密钥；凭证加密存储，含签名计数器防克隆 |
| Discord / X | OAuth 2.0 登录；注册关闭时未绑定账号的 OAuth 登录会被拒绝 |

### 会话与安全机制

- 登录成功后下发 `carryapi_session` 与 `carryapi_csrf` 两个 Cookie：`HttpOnly`、`SameSite=Lax`，会话有效期 **7 天**。
- **CSRF**：所有非 GET/HEAD/OPTIONS 的 session 写请求必须携带 `X-CSRF-Token` 头，值与 `carryapi_csrf` Cookie 一致（双提交 Cookie）。Passkey 注册完成接口因提交 CBOR 数据而豁免。
- **限流**：登录、注册、2FA 完成接口按客户端 IP 限流（每分钟 10 次，取 `X-Forwarded-For` 第一个值——前置可信代理时才准确）。
- 登录接口对"邮箱不存在/账号禁用/密码错误"统一返回相同 401，防止账号枚举。
- 禁用用户或关闭 TOTP 会立即吊销该用户全部会话。

### API Key（代理端点鉴权）

- 在「API Key」页创建，格式 `carry-<32 位十六进制>`。
- 数据库仅存哈希与前缀；**明文只在创建时显示一次**，关闭弹窗后无法再查看，请立即复制保存。
- 可编辑标签、启用/禁用、删除。

### 配额

- 按用户或 Key 设置 token / 费用上限，周期 `day` / `month` / `total`。
- 超限时代理端点返回 `429 quota_exceeded`。
- 当前后台界面只能编辑当前登录用户自身的配额（见 7.13）。

---

## 12. 运维

### 备份

单文件 `carryapi.db`（以及同目录下的 `carryapi.db-wal`、`carryapi.db-shm`），停服后一并拷贝即可。**同时备份主密钥** `carryapi.key`（或 `CARRYAPI_MASTER_KEY`），否则加密的上游 Key 无法解密。

> 当前版本没有"导出配置 JSON"的功能，配置迁移请直接备份数据库文件。

### 升级

替换二进制重启即可。SQLite 表结构变更通过版本化迁移自动处理（`schema_version` 表，当前 4 个版本）。启动时自动执行迁移。

### 健康检查

```bash
curl http://localhost:8067/api/health
# {"status":"ok"}
```

### 优雅关闭

进程收到 SIGINT / SIGTERM 后触发优雅关闭，最长等待 10 秒处理完在途请求。

### 安全

- 上游 API Key、TOTP 密钥、Passkey 凭证、OAuth 令牌均使用 AES-GCM 加密存储，主密钥来自 `CARRYAPI_MASTER_KEY` 或 `carryapi.key`。
- 代理端点始终要求 API Key 鉴权（无论广播开关状态）。
- 广播开启（默认 `all`，监听 `[::]` 双栈全部接口）时若未创建任何 API Key，建议尽快创建或加防火墙限制，避免被滥用。
- Passkey 公网部署务必正确设置 `CARRYAPI_RP_ID` 与 `CARRYAPI_RP_ORIGIN`（HTTPS）。

---

## 13. 常见问题

**Q: 端口被占用怎么办？**
用 `CARRYAPI_PORT=其他端口` 或 `scripts/run.sh 其他端口`（Windows 用 `run.bat`）指定。

**Q: 不知道管理员密码？**
删除 `carryapi.db`（连同 `carryapi.db-wal`/`carryapi.db-shm`）后重新启动，访问网页会重新进入「首次设置」向导，由你重新设置管理员邮箱与密码（会清空全部数据）。

**Q: 前端打不开 / 空白？**
确认运行的是单二进制（`go build` 后运行）而非仅后端。开发模式访问前端 `http://localhost:5173`，生产模式访问 `http://localhost:8067/`。

**Q: 调用代理端点报 model not found？**
自定义模型未配置、未启用，或没有启用的上游绑定。先在「模型管理」配置供应商 + 模型 + 定价并启用。

**Q: 广播开但其他设备访问不了？**
确认后台监听模式为 `all` 或 `::`，保存后已重启；Windows 防火墙和路由器/安全组需放行端口。IPv6 访问地址必须是 `http://[IPv6]:8067/`，且地址为公网 GUA 地址（通常 `2` 开头），不能使用 `fe80:` 链路本地地址或 `fd` 开头的内网地址。路由器/光猫的 IPv6 入站防火墙也必须允许该端口。若系统禁用了 IPv4-mapped IPv6，可改用 `--host 0.0.0.0` 先验证 IPv4。

**Q: TOTP 连续输错被锁？**
连续 5 次验证失败会锁定 15 分钟，等待后再试；或使用一次性备份码登录。

**Q: Passkey 登录用不了？**
公网部署必须设置 `CARRYAPI_RP_ID`（域名）与 `CARRYAPI_RP_ORIGIN`（HTTPS 地址），并使用受信任的 HTTPS 证书；浏览器需支持 WebAuthn。

**Q: OAuth 登录按钮不出现？**
需在 `settings` 表配置对应平台完整的 client id / client secret / redirect url，三项齐全且重启后才会启用。

---

## 14. 维护约定

> **重要**：每次**新增或减少功能**，必须同步修改本文档（以及 README），包括：
> 1. 新端点 / 新配置 / 新页面 → 更新对应章节（代理端点、配置、管理后台总览、前端操作指南、统计等）。
> 2. 启动方式变化 → 更新第 3 节，并同步 `scripts/` 脚本与 `Makefile`。
> 3. 新增/删除环境变量或 settings 键 → 更新第 5 节（含"是否生效"标注）。
> 4. 认证 / 权限 / 配额行为变化 → 更新第 11 节。
> 5. 占位功能一旦真正实现 → 去掉第 5、7 节中的"未生效"标注。

任何代码改动若改变了用户可见行为、CLI、API、配置或部署方式，都应让本文档与 README 保持一致。
