# carryAPI 子项目 6:前端管理后台 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现完整的 Vue 3 + Naive UI 管理后台,把子项目 2-5 的所有后端 API 串成可视化界面:登录/注册/2FA/Passkey/OAuth、模型/供应商/定价配置、API Key、统计图表、请求日志、系统设置(广播开关)、用户管理。构建产物 `web/dist/` 被 Go `//go:embed` 嵌入单二进制。

**Architecture:** 前端工程在 `web/`(子项目 1 已建占位)。Vue 3 + TypeScript + Vite + Pinia + Vue Router + Naive UI + axios + ECharts。分层:
- `src/api/`:axios 封装 + 各资源 API 模块(单例)。
- `src/stores/`:Pinia store(session/用户/设置)。
- `src/router/`:路由 + 守卫(session + 角色)。
- `src/views/`:页面组件。
- `src/components/`:可复用组件(表格/图表/对话框)。
- `src/utils/`:CSRF、日期、格式化 helper。

**契约对齐**(基于后端实际路由,子项目 2-5 已实现):
- 登录/注册/2FA/Passkey/登出/OAuth:见 `internal/server/router.go`。
- 管理端:providers/models/prices/keys/quotas/users/settings/stats/logs。
- **账号设置页收敛**:后端只有 `/2fa/setup`、`/2fa/disable`(需密码)、无独立"改密码"端点、无 Passkey 管理/解绑 OAuth/会话吊销端点。前端账号设置页只实现**后端实际支持**的能力:显示基本信息(来自 `/me`)、开关 TOTP、显示已绑定登录方式(来自 `/me` 的 auth_methods)。改密码/Passkey 管理/解绑 OAuth 标注为"后续"(后端未提供),不假想不存在的端点。

**Tech Stack:** Vue 3 + TypeScript、Vite 5、Pinia、Vue Router 4、Naive UI 2、axios、ECharts、`//go:embed`(已有)。

## Global Constraints

- 前端必须能 `npm run build` 产出 `web/dist/`,且被 `web` 包的 `//go:embed` 嵌入后,Go 二进制 `GET /` 返回完整前端。
- 开发:`npm run dev`(Vite 代理 `/api` + `/v1` 到 `127.0.0.1:8080`,子项目 1 已配)。
- **CSRF**:所有非 GET 的 `/api/*` 请求必须带 `X-CSRF-Token` 头(值 = `carryapi_csrf` cookie)。axios 拦截器自动从 cookie 读并设置。
- **鉴权双轨**:管理端用 session cookie(登录后自动携带);代理端点 `/v1/*` 用 API Key(前端不直接调用,只在 API Key 管理页展示)。前端不调用 `/v1/*`。
- 401 处理:axios 拦截器统一跳登录页。
- 角色:普通用户不显示/访问 admin 菜单(`providers/models/quotas/users/settings` 等);后端已强制 403,前端隐藏只是 UX。
- 广播开关改 `settings.listen_host` 后需重启进程——前端提示"已保存,需重启生效",不假想后端自动重启(子项目 1 无该机制)。
- TDD:前端组件逻辑(store/api/工具)用 Vitest 单测;页面组件做冒烟(渲染不报错)。纯 UI 交互不强制单测。
- Git 身份:`rayafriandion <amizhisa@outlook.com>`(本仓库已配置)。

---

## File Structure

```
web/
├── index.html                  # (子项目1已有,改标题)
├── package.json                # (子项目1已有,加依赖)
├── vite.config.ts              # (子项目1已有,加 alias + vitest)
├── tsconfig.json               # (子项目1已有,补 types)
├── src/
│   ├── main.ts                 # (子项目1占位,改为完整 app 入口)
│   ├── App.vue                 # 根组件(路由视图 + Naive 全局配置)
│   ├── api/
│   │   ├── http.ts             # axios 实例 + 拦截器(CSRF + 401 + 错误)
│   │   ├── auth.ts             # login/register/2fa/logout/me/passkey
│   │   ├── catalog.ts          # providers/models/prices
│   │   ├── keys.ts             # api keys CRUD
│   │   ├── quota.ts            # quotas
│   │   ├── users.ts            # users CRUD(admin)
│   │   ├── settings.ts         # settings get/update
│   │   ├── stats.ts            # summary/trend/cost/success-rate/logs
│   │   └── index.ts            # 聚合导出
│   ├── stores/
│   │   ├── auth.ts             # session/user/auth_methods/角色
│   │   └── settings.ts         # 全局设置缓存
│   ├── router/
│   │   └── index.ts            # 路由 + 守卫(登录 + 角色)
│   ├── utils/
│   │   ├── csrf.ts             # 读 carryapi_csrf cookie
│   │   ├── date.ts             # 时间格式化
│   │   └── csv.ts              # 导出 CSV
│   ├── views/
│   │   ├── LoginView.vue       # 登录/注册/2FA/Passkey/OAuth
│   │   ├── DashboardView.vue   # 仪表盘
│   │   ├── StatsView.vue       # 统计(汇总/趋势/费用/成功率)
│   │   ├── LogsView.vue        # 请求日志
│   │   ├── ModelsView.vue      # 模型管理(供应商+模型+定价)
│   │   ├── KeysView.vue        # API Key 管理
│   │   ├── QuotasView.vue      # 配额管理(admin)
│   │   ├── UsersView.vue       # 用户管理(admin)
│   │   ├── SettingsView.vue    # 系统设置(admin,含广播开关)
│   │   └── AccountView.vue     # 账号设置(TOTP/基本信息)
│   ├── components/
│   │   ├── AppLayout.vue       # 顶部导航 + 侧边菜单(按角色)
│   │   ├── StatCard.vue        # 仪表盘指标卡
│   │   ├── TrendChart.vue      # 趋势折线图(ECharts)
│   │   └── SuccessRateTable.vue # 成功率表格(下钻)
│   └── assets/
│       └── logo.svg
```

---

### Task 1: 前端工程基座(依赖 + api/http + csrf + stores/auth + router)

**Files:**
- Modify: `web/package.json`、`web/vite.config.ts`、`web/tsconfig.json`
- Modify: `web/index.html`、`web/src/main.ts`
- Create: `web/src/App.vue`、`web/src/api/http.ts`、`web/src/utils/csrf.ts`、`web/src/stores/auth.ts`、`web/src/router/index.ts`
- Test: `web/src/utils/csrf.spec.ts`、`web/src/stores/auth.spec.ts`(Vitest)

**Interfaces:**
- Produces:
  - `web/src/api/http.ts`:导出 `http` axios 实例(baseURL 空,相对路径);请求拦截器:非 GET 自动加 `X-CSRF-Token`(读 cookie);响应拦截器:401 清 store 跳 `/login`,其他错误 reject。
  - `web/src/utils/csrf.ts`:`getCSRFToken() string`(从 `document.cookie` 读 `carryapi_csrf`)。
  - `web/src/stores/auth.ts`:`useAuthStore`(Pinia):state {user, authMethods, status},actions {login, register, complete2FA, logout, fetchMe, setupTOTP, disableTOTP},getter {isAdmin}。
  - `web/src/router/index.ts`:路由表(login + 各视图),守卫:未登录访问受保护路由 -> `/login`;admin 路由校验 `isAdmin`。
- 依赖:`vue-router`、`pinia`、`naive-ui`(子项目1已有)、`axios`、`echarts`、`@vue/test-utils`、`vitest`、`jsdom`。

- [ ] **Step 1: 安装依赖**

```bash
cd /d/Projects/carryAPI/web
npm install vue-router pinia axios echarts
npm install -D vitest @vue/test-utils jsdom
```

- [ ] **Step 2: 写 csrf 单测(失败)**

`web/src/utils/csrf.spec.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { getCSRFToken } from './csrf'

describe('getCSRFToken', () => {
  beforeEach(() => {
    // jsdom 里 document.cookie 可读写
    document.cookie = 'carryapi_csrf=abc123; path=/'
  })
  it('reads the csrf token from cookie', () => {
    expect(getCSRFToken()).toBe('abc123')
  })
  it('returns empty string when cookie absent', () => {
    document.cookie = 'carryapi_csrf=; expires=Thu, 01 Jan 1970 00:00:00 GMT'
    expect(getCSRFToken()).toBe('')
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

```bash
cd /d/Projects/carryAPI/web
npx vitest run
```

预期:编译失败(找不到 csrf 模块)。

- [ ] **Step 4: 实现 csrf.ts**

`web/src/utils/csrf.ts`:

```ts
const COOKIE_NAME = 'carryapi_csrf'

export function getCSRFToken(): string {
  if (typeof document === 'undefined') return ''
  const match = document.cookie.match(new RegExp('(?:^|; )' + COOKIE_NAME + '=([^;]*)'))
  return match ? decodeURIComponent(match[1]) : ''
}
```

- [ ] **Step 5: 实现 http.ts**

`web/src/api/http.ts`:

```ts
import axios from 'axios'
import { getCSRFToken } from '../utils/csrf'

export const http = axios.create({ baseURL: '' })

http.interceptors.request.use((config) => {
  const method = (config.method || 'get').toLowerCase()
  if (method !== 'get' && method !== 'head' && method !== 'options') {
    const token = getCSRFToken()
    if (token) config.headers['X-CSRF-Token'] = token
  }
  return config
})

http.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) {
      // 登录失效 -> 跳登录(通过事件或 window.location)
      window.location.href = '/login'
    }
    return Promise.reject(err)
  }
)

// 统一错误消息提取
export function errorMessage(err: unknown): string {
  const data = (err as any)?.response?.data
  return data?.error || data?.message || '请求失败'
}
```

- [ ] **Step 6: 实现 stores/auth.ts + 单测**

`web/src/stores/auth.ts`:

```ts
import { defineStore } from 'pinia'
import http from '../api/http'

export interface UserInfo {
  id: number
  email: string
  role: string
  status: string
}

export const useAuthStore = defineStore('auth', {
  state: () => ({
    user: null as UserInfo | null,
    authMethods: [] as string[],
    initialized: false,
  }),
  getters: {
    isAdmin: (s) => s.user?.role === 'admin',
    isLoggedIn: (s) => !!s.user,
  },
  actions: {
    async login(email: string, password: string) {
      const res = await http.post('/api/auth/login', { email, password })
      if (res.data.requires_2fa) {
        return { requires2fa: true }
      }
      this.setUser(res.data.user)
      return { requires2fa: false }
    },
    async complete2FA(email: string, code: string) {
      const res = await http.post('/api/auth/2fa/complete', { email, code })
      this.setUser(res.data.user)
    },
    async register(email: string, password: string) {
      await http.post('/api/auth/register', { email, password })
    },
    async logout() {
      try { await http.post('/api/auth/logout') } catch { /* ignore */ }
      this.user = null
      this.authMethods = []
    },
    async fetchMe() {
      const res = await http.get('/api/auth/me')
      this.setUser(res.data.user)
      this.authMethods = res.data.auth_methods || []
      this.initialized = true
    },
    setUser(u: UserInfo) {
      this.user = u
      this.initialized = true
    },
    async setupTOTP() {
      const res = await http.post('/api/auth/2fa/setup')
      return res.data // {secret, otpauth_url, backup_codes}
    },
    async disableTOTP(password: string) {
      await http.post('/api/auth/2fa/disable', { password })
    },
  },
})
```

`web/src/stores/auth.spec.ts`(mock http):

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useAuthStore } from './auth'

vi.mock('../api/http', () => ({
  default: {
    post: vi.fn(),
    get: vi.fn(),
  },
}))

import http from '../api/http'

describe('auth store', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
    vi.clearAllMocks()
  })
  it('login without 2fa sets user', async () => {
    (http.post as any).mockResolvedValue({ data: { user: { id: 1, email: 'a@x.com', role: 'admin' } } })
    const s = useAuthStore()
    const r = await s.login('a@x.com', 'pw')
    expect(r.requires2fa).toBe(false)
    expect(s.user?.email).toBe('a@x.com')
    expect(s.isAdmin).toBe(true)
  })
  it('login requiring 2fa does not set user', async () => {
    (http.post as any).mockResolvedValue({ data: { requires_2fa: true } })
    const s = useAuthStore()
    const r = await s.login('a@x.com', 'pw')
    expect(r.requires2fa).toBe(true)
    expect(s.user).toBeNull()
  })
  it('logout clears user', async () => {
    (http.post as any).mockResolvedValue({})
    const s = useAuthStore()
    s.setUser({ id: 1, email: 'a@x.com', role: 'user', status: 'active' })
    await s.logout()
    expect(s.user).toBeNull()
  })
})
```

- [ ] **Step 7: 实现 router/index.ts + App.vue + main.ts**

`web/src/router/index.ts`(路由守卫骨架):

```ts
import { createRouter, createWebHistory } from 'vue-router'
import { useAuthStore } from '../stores/auth'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/login', name: 'login', component: () => import('../views/LoginView.vue') },
    { path: '/', component: () => import('../components/AppLayout.vue'), children: [
      { path: '', name: 'dashboard', component: () => import('../views/DashboardView.vue') },
      { path: 'stats', name: 'stats', component: () => import('../views/StatsView.vue') },
      { path: 'logs', name: 'logs', component: () => import('../views/LogsView.vue') },
      { path: 'keys', name: 'keys', component: () => import('../views/KeysView.vue') },
      { path: 'account', name: 'account', component: () => import('../views/AccountView.vue') },
      // admin-only
      { path: 'models', name: 'models', component: () => import('../views/ModelsView.vue'), meta: { admin: true } },
      { path: 'quotas', name: 'quotas', component: () => import('../views/QuotasView.vue'), meta: { admin: true } },
      { path: 'users', name: 'users', component: () => import('../views/UsersView.vue'), meta: { admin: true } },
      { path: 'settings', name: 'settings', component: () => import('../views/SettingsView.vue'), meta: { admin: true } },
    ]},
    { path: '/:pathMatch(.*)*', redirect: '/' },
  ],
})

router.beforeEach(async (to) => {
  const auth = useAuthStore()
  if (!auth.initialized) {
    try { await auth.fetchMe() } catch { /* 未登录 */ }
  }
  if (to.name === 'login') return true
  if (!auth.isLoggedIn) return { name: 'login' }
  if (to.meta.admin && !auth.isAdmin) return { name: 'dashboard' }
  return true
})

export default router
```

`web/src/App.vue`:

```vue
<template>
  <n-config-provider :theme="null">
    <n-message-provider>
      <n-dialog-provider>
        <router-view />
      </n-dialog-provider>
    </n-message-provider>
  </n-config-provider>
</template>

<script setup lang="ts">
import { NConfigProvider, NMessageProvider, NDialogProvider } from 'naive-ui'
</script>
```

`web/src/main.ts`(替换占位):

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'

createApp(App).use(createPinia()).use(router).mount('#app')
```

- [ ] **Step 8: 构建验证 + 提交**

```bash
cd /d/Projects/carryAPI/web
npx vitest run        # csrf + auth store 测试通过
npm run build         # 生成 dist
cd /d/Projects/carryAPI
go build -o carryapi.exe ./cmd/carryapi   # 嵌入验证
./carryapi.exe & sleep 1; curl -s http://127.0.0.1:8080/ | head -3; kill %1
```

预期:测试通过;`/` 返回新前端 HTML。

```bash
git add web/package.json web/vite.config.ts web/tsconfig.json web/index.html web/src cmd/carryapi
git commit -m "feat(web): frontend base with http client, csrf, auth store, router"
```

---

### Task 2: 登录/注册/2FA/Passkey 视图

**Files:**
- Create: `web/src/views/LoginView.vue`
- Create: `web/src/api/auth.ts`

**内容:**
- 登录表单(邮箱+密码),注册切换(若 `registration_open`),TOTP 码输入(2FA 步骤),Passkey 登录(调 `passkey/login/begin` + `navigator.credentials.get`),Discord/X 登录按钮(链接到 `/api/auth/oauth/{provider}`)。
- `web/src/api/auth.ts`:封装 store 之外的 Passkey 调用:

```ts
import http from './http'

export async function passkeyLoginBegin(email: string): Promise<{publicKey: any; session_key: string}> {
  const res = await http.post('/api/auth/passkey/login/begin', null, { params: { email } })
  return res.data
}

export async function passkeyLoginFinish(email: string, sessionKey: string, assertion: any): Promise<void> {
  // assertion 作为 JSON body 提交,后端 FinishLogin 从 request body 解析
  await http.post('/api/auth/passkey/login/finish', assertion, { params: { email, session_key: sessionKey } })
}
```

> 注:后端 PasskeyHandler 的 finish 从 `r.Body` 解析 WebAuthn 凭证(用 `webauthn.FinishLogin` 的 protocol.ParseCredentialRequestResponse(r)),所以前端把 `navigator.credentials.get()` 返回的 `PublicKeyCredential` JSON(需 `toJSON()`)作为 body 发送即可。Passkey 完整联调依赖浏览器 WebAuthn,单测/冒烟只验证 begin 端点可达与界面渲染。

- 登录成功跳 `/`;`requires_2fa` 时显示 TOTP 输入。
- 注册成功跳登录。
- **无单测**(UI 交互 + WebAuthn 浏览器依赖),只做渲染冒烟 + `npm run build`。

- [ ] **Step 1: 实现 LoginView.vue + auth.ts**

按上方 + Naive UI 表单组件。关键:登录调 store.login,2FA 调 store.complete2FA,注册调 store.register + 提示后切登录。

- [ ] **Step 2: 构建 + 冒烟 + 提交**

```bash
npx vitest run && npm run build
git add web/src/views/LoginView.vue web/src/api/auth.ts
git commit -m "feat(web): login/register/2fa/passkey view"
```

---

### Task 3: 布局 + 仪表盘 + 统计视图

**Files:**
- Create: `web/src/components/AppLayout.vue`、`web/src/components/StatCard.vue`、`web/src/components/TrendChart.vue`
- Create: `web/src/views/DashboardView.vue`、`web/src/views/StatsView.vue`
- Create: `web/src/api/stats.ts`

**内容:**
- `AppLayout.vue`:顶部导航(logo、当前用户邮箱、登出)+ 侧边菜单(按角色过滤:普通用户只看 Dashboard/统计/日志/Key/账号;admin 加模型/配额/用户/设置)。
- `web/src/api/stats.ts`:封装 summary/trend/cost/success-rate/logs 调用 + 时间参数。
- `DashboardView.vue`:调 summary(今日),用 StatCard 展示请求数/Token/费用/成功率;TrendChart 近 7 天趋势;最近日志表(前 10)。
- `StatsView.vue`:Tab 切换 汇总/趋势/费用/成功率。汇总按模型/上游/Key 表格;趋势折线图(granularity day/hour + 时间范围);费用表格 + CSV 导出;成功率表格(SuccessRateTable 下钻:点失败数 -> 按 error_type 分组)。
- `TrendChart.vue`:ECharts 折线图(请求量 + 成功率双轴)。
- 无单测(纯 UI + 图表),构建冒烟。

- [ ] **Step 1: 实现布局 + API + 视图**

按上述。用 Naive UI 的 `n-layout`、`n-menu`、`n-tabs`、`n-data-table`、`n-statistic`;ECharts 在 TrendChart 内 `echarts.init` + watch。

- [ ] **Step 2: 构建 + 提交**

```bash
npm run build
git add web/src/components/AppLayout.vue web/src/components/StatCard.vue web/src/components/TrendChart.vue web/src/views/DashboardView.vue web/src/views/StatsView.vue web/src/api/stats.ts
git commit -m "feat(web): layout, dashboard, stats views with charts"
```

---

### Task 4: 日志视图 + CSV 导出工具

**Files:**
- Create: `web/src/views/LogsView.vue`
- Create: `web/src/utils/csv.ts`

**内容:**
- `LogsView.vue`:分页表格(page/page_size),筛选(model/status/error_type/request_id/时间范围);列:时间/RequestID/用户/模型/上游/协议/输入输出 token/费用/耗时/状态/错误。点 RequestID 复制。
- `csv.ts`:`exportCSV(filename, headers, rows)` 生成 Blob 下载。
- 成功率页和费用页复用 exportCSV。

- [ ] **Step 1: 实现 + 单测 csv.ts**

`web/src/utils/csv.spec.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { toCSV } from './csv'

describe('toCSV', () => {
  it('builds csv with header and escaped values', () => {
    const csv = toCSV(['name', 'note'], [{ name: 'a', note: 'x,y' }])
    expect(csv).toContain('name,note')
    expect(csv).toContain('"x,y"')
  })
})
```

`web/src/utils/csv.ts`:

```ts
export function toCSV(headers: string[], rows: Record<string, any>[]): string {
  const esc = (v: any) => {
    const s = String(v ?? '')
    return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s
  }
  const lines = [headers.map(esc).join(',')]
  for (const row of rows) lines.push(headers.map((h) => esc(row[h])).join(','))
  return lines.join('\n')
}

export function downloadCSV(filename: string, csv: string) {
  const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  a.click()
  URL.revokeObjectURL(url)
}
```

- [ ] **Step 2: 构建 + 提交**

```bash
npx vitest run && npm run build
git add web/src/views/LogsView.vue web/src/utils/csv.ts web/src/utils/csv.spec.ts
git commit -m "feat(web): logs view with filters and csv export"
```

---

### Task 5: API Key 管理视图

**Files:**
- Create: `web/src/views/KeysView.vue`
- Create: `web/src/api/keys.ts`

**内容:**
- 表格:前缀/标签/状态/创建时间/最后使用。
- 创建:弹窗输入标签 -> 调 `/api/keys` -> 返回明文 -> 一次性展示(复制按钮 + 警告"关闭后无法再次查看")。
- 编辑标签/禁用/删除。
- 删除/禁用需确认。

- [ ] **Step 1: 实现 + 构建 + 提交**

```bash
npm run build
git add web/src/views/KeysView.vue web/src/api/keys.ts
git commit -m "feat(web): api key management view"
```

---

### Task 6: 模型/供应商/定价管理视图

**Files:**
- Create: `web/src/views/ModelsView.vue`
- Create: `web/src/api/catalog.ts`

**内容:**
- `ModelsView.vue` 三个 Tab:
  - 上游供应商:表格(名称/URL/协议/状态)+ 增删改弹窗(API Key 输入框,type=password)。
  - 自定义模型:表格(名称/映射上游/真实模型名/启用)+ 增删改弹窗 + 启用开关。
  - 模型定价:选模型 -> 编辑当前价格(输入/输出/缓存读写)+ 价格历史列表。

- [ ] **Step 1: 实现 + 构建 + 提交**

```bash
npm run build
git add web/src/views/ModelsView.vue web/src/api/catalog.ts
git commit -m "feat(web): provider/model/price management view"
```

---

### Task 7: 用户/配额管理视图(admin)

**Files:**
- Create: `web/src/views/UsersView.vue`、`web/src/views/QuotasView.vue`
- Create: `web/src/api/users.ts`、`web/src/api/quota.ts`

**内容:**
- `UsersView.vue`:用户表格(邮箱/角色/状态/创建时间)+ 创建用户(admin 指定角色)+ 改角色/禁用/删除(删除需确认,不能删自己)。
- `QuotasView.vue`:按用户/Key 配额表格 + 编辑(token/费用上限 + 周期)。

- [ ] **Step 1: 实现 + 构建 + 提交**

```bash
npm run build
git add web/src/views/UsersView.vue web/src/views/QuotasView.vue web/src/api/users.ts web/src/api/quota.ts
git commit -m "feat(web): user and quota management views (admin)"
```

---

### Task 8: 系统设置视图(广播开关)+ 账号设置视图

**Files:**
- Create: `web/src/views/SettingsView.vue`、`web/src/views/AccountView.vue`
- Create: `web/src/api/settings.ts`

**内容:**
- `SettingsView.vue`(admin):广播开关(改 `settings.listen_host`,提示"需重启生效")、开放注册开关、强制 2FA 开关、日志保留天数。OAuth client id/secret 显示(后端 settings GET 白名单不含 oauth_*——**只读展示后端返回的**;若后端不返回则不展示)。主密钥状态:后端无此端点,不展示。
- `AccountView.vue`:显示基本信息(邮箱/角色,来自 /me)+ 已绑定登录方式列表(auth_methods)+ 开启/关闭 TOTP(关闭需当前密码,调 disableTOTP)。

> **范围收敛说明**:设计文档账号设置页还想要"改密码/管理 Passkey/绑定解绑 OAuth/会话吊销",但后端子项目 2 只实现了 `2fa/setup`、`2fa/disable`(需密码)、`/me`。前端只实现后端实际支持的能力;其余标注"后续"。

- [ ] **Step 1: 实现 + 构建 + 提交**

```bash
npm run build
git add web/src/views/SettingsView.vue web/src/views/AccountView.vue web/src/api/settings.ts
git commit -m "feat(web): system settings (broadcast) and account views"
```

---

### Task 9: 构建验证 + README + 全量冒烟

**Files:**
- Modify: `README.md`

**内容:**
- 全量构建 + Go 嵌入冒烟:登录 -> 建 provider/model/price -> 建 key -> 调 /v1 统计页能看到数据。
- README 加"管理后台"章节:URL、默认 admin 登录、功能概览、开发模式。

- [ ] **Step 1: 全量验证**

```bash
cd /d/Projects/carryAPI/web
npx vitest run && npm run build
cd /d/Projects/carryAPI
go build -o carryapi.exe ./cmd/carryapi
# 冒烟:启动 + curl 首页 + /api/health
./carryapi.exe & sleep 2; curl -s http://127.0.0.1:8080/api/health; echo; curl -s http://127.0.0.1:8080/ | head -5; kill %1
go test ./... -count=1
```

预期:Go 全量测试仍绿(前端改动不影响 Go);首页返回新前端。

- [ ] **Step 2: 更新 README**

加"管理后台"章节:URL、默认 admin 登录、功能概览、开发模式(`npm run dev`)。

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs: admin console section in readme"
```

---

## 子项目 6 完成标准

- [ ] `npm run build` 成功,`web/dist/` 被 Go 嵌入,`GET /` 返回完整前端
- [ ] 登录/注册/2FA(TOTP)/登出可用;Passkey/OAuth 入口可达
- [ ] 仪表盘(请求数/Token/费用/成功率 + 趋势图 + 最近日志)
- [ ] 统计视图(汇总/趋势/费用 + CSV/成功率下钻)
- [ ] 请求日志(分页 + 筛选)
- [ ] API Key 管理(创建一次性明文 + 复制 + 编辑/禁用/删除)
- [ ] 模型/供应商/定价管理(admin)
- [ ] 用户/配额管理(admin)
- [ ] 系统设置(广播开关 + 开关项)+ 账号设置(TOTP/基本信息)
- [ ] 角色权限:普通用户看不到 admin 菜单/路由
- [ ] CSRF 自动携带;401 跳登录
- [ ] Go 全量测试仍绿(前端改动不破坏后端)
